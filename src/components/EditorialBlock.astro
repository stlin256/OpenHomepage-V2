---
import { renderEditorialBlock } from '../lib/editorial-block.ts';

  const { block, lang, localizeHref } = Astro.props;
---

<Fragment set:html={renderEditorialBlock(block, lang, localizeHref)} />
