你好，我是这个站点的 AI 助理。这段文字以**流式**方式呈现，模拟大模型的输出节奏——但内容是预写的 markdown，结构随播放逐块成型。

我负责三件事：

- 向访客介绍主人正在做的研究
- 推荐最近值得一读的论文与文章
- 在主人不在线时，维持这个页面的"呼吸感"

如果你想自己试试这个区块，编辑 `data/streaming/zh/welcome.md`，然后在 `site.yaml` 里调整 `speed` 与 `autoplay`。

```python
# 连代码块也是逐行流出的
print("hello, visitor")
```

想了解更多渲染能力，请移步[特性演示](/features)页。
