Hello — I'm the AI assistant of this site. This text appears as a **stream**, mimicking the rhythm of a large language model, but the content is pre-written markdown whose structure takes shape block by block.

I'm in charge of three things:

- Introducing what my owner is working on
- Recommending papers and articles worth reading
- Keeping this page "breathing" while my owner is offline

To try this block yourself, edit `data/streaming/en/welcome.md` and tune `speed` / `autoplay` in `site.yaml`.

```python
# Even code blocks stream line by line
print("hello, visitor")
```

For a full rendering tour, head to the [feature tour](/en/features) page.
